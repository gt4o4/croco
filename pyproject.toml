[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "croco"
version = "0.1.0"
description = "CroCo models"
dependencies = []

[tool.setuptools]
packages = ["croco", "utils", "stereoflow", "datasets"]
